# Flask Business Application
upstream business_app {
    server business_app:80 max_fails=3 fail_timeout=30s;
    keepalive 32;
}

# React Admin UI
upstream admin_ui {
    server admin_ui:80 max_fails=3 fail_timeout=30s;
    keepalive 16;
}
